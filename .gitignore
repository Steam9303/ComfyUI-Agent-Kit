# Python
__pycache__/
*.pyc

# Fetched at install time: never commit (huge / third-party)
/templates/
/workflow_templates/
comfyui-agent-kit-data/
comfyui-claude-kit-data/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Generated outputs
shared/comfyui/workflows/*.json
!shared/comfyui/workflows/.gitkeep

# Project start-policy config (downstream projects commit their own; the example ships)
.comfyui-agent.json

# Local-only chart generators + this project's operating manual.
# Only the rendered PNGs in docs/assets/ go public; the HTML templates stay with us for making releases.
/tools/
/CLAUDE.md
